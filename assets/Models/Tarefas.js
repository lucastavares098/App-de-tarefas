export default class Tarefas {
    constructor(descricao, data_termino, prioridade, concluido) {
        this.descricao = descricao;
        this.data_termino = data_termino;
        this.prioridade = prioridade;
        this.concluido = concluido;
    }
}